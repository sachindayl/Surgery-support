import 'package:flutter/material.dart';

class CreatePatientView extends StatelessWidget {

  @override
  Widget build(BuildContext context) {
    return Scaffold(
      body: SafeArea(
        child: Container(
          child: Text('Create patient view'),
        ),
      ),
    );
  }
}